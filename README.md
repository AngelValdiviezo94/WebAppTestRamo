Ajustar la clase "Consumo" con la ruta con la que se levanta el proyecto del api, esto se debe realizar debido que el host puede variar,
esto se encuentra en la variable "serviceUrl" y se debe cambiar su valor con el host de la máquina donde se prueba 
por ejemplo: https://localhost:2911....
En el proyecto existe una ruta que sirve para mi escenario local, pero para realizar sus pruebas deben ajustar esta variable
